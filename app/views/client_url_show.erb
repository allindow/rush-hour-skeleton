<div class="container col-md-12" class="page-title">
  <h2><%= (@identifier.root_url + "/#{@relativepath}") %></h2>
</div>

<div class="well page-title">
  <ul>
    <% UrlChecker.content(@identifier.root_url + "/#{@relativepath}").each do |stat| %>
    <li><%= stat %></li>
    <%end%>
  </ul>
</div>
